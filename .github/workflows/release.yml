name: Release

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:
    inputs:
      release_type:
        description: 'Release type'
        required: true
        default: 'patch'
        type: choice
        options:
        - patch
        - minor
        - major

jobs:
  release:
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch' || (github.event_name == 'push' && contains(github.event.head_commit.message, '[release]'))
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
        token: ${{ secrets.GITHUB_TOKEN }}
        
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        cache: 'yarn'
        registry-url: 'https://registry.npmjs.org'
        
    - name: Install dependencies
      run: yarn install --frozen-lockfile
      
    - name: Run tests
      run: yarn test
      
    - name: Generate documentation
      run: yarn run document
      
    - name: Configure Git
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
    
    # Placeholder for semantic-release or manual npm version bump
    - name: Version bump and publish
      run: |
        echo "🚧 Release workflow configured but not yet active"
        echo "To enable automatic publishing:"
        echo "1. Add NPM_TOKEN secret to repository"
        echo "2. Configure semantic-release or manual versioning"
        echo "3. Uncomment the publishing steps below"
      
    # Uncomment these steps when ready for automatic publishing:
    # - name: Bump version
    #   run: yarn version --new-version ${{ github.event.inputs.release_type || 'patch' }}
    #   
    # - name: Publish to npm
    #   run: yarn publish --non-interactive
    #   env:
    #     NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
    #     
    # - name: Push version changes
    #   run: git push --follow-tags